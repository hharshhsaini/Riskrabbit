REMAINING — Predictive Deployment Risk Assessment System
Running list of everything deferred. Nothing here is forgotten; it is parked.
Last updated: 2026-09-14, Step 22 (frontend polish pass) done

================================================================
A. BLOCKED ON YOU — needed before the next steps can run
================================================================

[x] A1. DONE 2026-09-13 — backend/.env — replace DATABASE_URL placeholder with the real Neon
        POOLED connection string (the host with "-pooler" in it).
        Neon project exists (Singapore, branch production, db neondb,
        pooling ON). Copy the string with the Copy button, not by selecting
        the masked text. channel_binding=require is fine — psycopg2 bundles
        libpq 17.
        Blocks: Step 02 live gate, and everything from Step 05 onward.

[x] A2. DONE 2026-09-13 (fine-grained, verified limit=5000, write refused) — backend/.env — replace GITHUB_TOKEN with a real PAT.
        Use a token with NO write scopes — reading public repos needs none,
        and authentication alone is what raises the rate limit to 5,000/hr.
        Preferred: fine-grained token, "Public Repositories (read-only)".
        Acceptable: classic token with every scope box left UNCHECKED.
        NOTE: BUILD-PROMPTS.md says public_repo scope — that grants PUSH
        access to all your public repos and is more than this project needs.
        Blocks: Step 04 verification, Step 09 data collection.

[x] A3. DONE 2026-09-13 — LLM key. Using Groq free tier instead of OpenAI,
        through the same openai library (no new dependency).
        backend/.env: OPENAI_API_KEY=<groq key>,
        OPENAI_BASE_URL=https://api.groq.com/openai/v1,
        LLM_MODEL=openai/gpt-oss-20b. config.py and .env.example updated.
        Verified 2026-09-13: settings reads the key, real completion works.
        Still to do in Step 15: pass settings.OPENAI_BASE_URL and
        settings.LLM_MODEL when creating the OpenAI client.
        !! MUST pass reasoning_effort="low" for gpt-oss models. Tested: with
        the default effort and max_tokens=200, the model spent all 200 tokens
        on hidden reasoning and returned EMPTY content (finish=length). With
        reasoning_effort="low" it used 71 tokens and returned a good answer.
        An empty explanation would silently reach the dashboard.
        Also for Step 16: the test answer said "scheduled over the weekend"
        and "critical failures" — slight overstatement, exactly what the
        faithfulness check must catch.
        Tell mentor the explanation step runs on an OpenAI-compatible API
        (Groq), not OpenAI itself — PRD page 1 says "OpenAI API".

[ ] A4. ROTATE BOTH CREDENTIALS — the Neon password and the GitHub token
        were pasted into a chat on 2026-09-13, so treat them as exposed.
        (The Groq key was pasted only into .env, not chat — no rotation needed.)
        - Neon: Connect dialog -> "Reset password" -> paste new string in .env
        - GitHub: Settings -> Developer settings -> Fine-grained tokens ->
          delete riskrabbit token -> create a new read-only one -> .env
        Deferred by Harsh on 2026-09-13 — do at the end with the rest of this list.
        Next time, paste secrets straight into backend/.env yourself.

================================================================
B. OPEN GATES — steps done in code but not yet fully verified
================================================================

[x] B1. DONE 2026-09-13 — Neon PG 18.6: 4 tables, all indexes/unique/check constraints present, 2 startups -> users count 1. Step 02 live gate. Once A1 is done:
          cd backend && ./venv/bin/uvicorn main:app --reload
        Expect "Application startup complete".
        Then in the Neon SQL editor confirm:
          - 4 tables exist (users, repositories, pull_requests, predictions)
          - users has exactly 1 row
        Then restart the app and confirm users STILL has 1 row
        (the seed must not create a second user).

        Alternative if you would rather not touch Neon yet: start Docker
        Desktop and I will verify create_all + the seed against a throwaway
        local Postgres container instead.

[ ] B2. Step 09 gate — collection is running (started 2026-09-14 ~00:40).
        Already verified on a scratch copy: kill -9 mid-run plus a
        half-written last line, then restart -> resumed, repaired the line,
        no duplicates.
        [x] An hour without crashing — confirmed 2026-09-14 01:40: running
            since 00:39:56, 0 errors, 0 rate-limit sleeps, 1,211 PRs and
            7,623 commits written, 4 of 16 repos complete.
        [x] Final count in range — collection finished 2026-09-14 11:56:
            4,594 PRs from all 16 repos, 27,126 commits. Clean exit, "DONE".
            (starlette has only 176 eligible PRs and typer 218, not 300.)
        Expected runtime: roughly 5 hours. About 14k API calls for PRs plus
        ~9k for commit file lists, against 5,000/hour; the script sleeps
        through rate limits on its own.
        Useful commands (run from backend/):
          wc -l ml/data/raw_prs.jsonl          # PRs collected so far
          tail -f ml/data/collect.log          # live progress
          kill $(cat ml/data/collect.pid)      # stop
          nohup caffeinate -i ./venv/bin/python -m ml.collect >> ml/data/collect.log 2>&1 &
                                               # restart / resume
        If the laptop sleeps or the process stops, just run the restart
        command — it resumes where it left off.
        encode/starlette now redirects to Kludex/starlette; the list uses the
        new name.

[x] B3. Step 10 gate DONE on the full data (with the D0v backport fix):
        163 of 4,594 = 3.5% risky — BELOW the 5% floor, not broken. 20 examples
        hand-checked: ~4-5 of 10 risky labels are genuine follow-up fixes or
        reverts; negatives look genuinely uneventful. Accepted as a documented
        limitation (docs/model-ceiling.md).
[ ] B3 (old text). Step 10 gate — FINAL RE-RUN NEEDED after collection finishes.
        Current result covers only the 3 repos fully collected so far
        (mypy, sentry-python, netbox; 900 PRs): 49 risky = 5.4%, inside the
        5-15% gate. A background job re-runs ml.label automatically when the
        collector exits and writes ml/data/label_summary.txt. Then:
          ./venv/bin/python -m ml.label --inspect 20
        and hand-check the 20 examples again across all 16 repos.

[x] B4. Step 11 gate DONE on full data: PASS, 9 features separate the classes.
        Strongest: author_pr_count 0.687 (label-biased), lines_added 0.625,
        total_churn 0.623, files_changed 0.620, unique_dirs_touched 0.607,
        lines_removed 0.602, pr_age_hours 0.399 (LOWER when risky: median 3.4h
        risky vs 17.5h safe). review_comment_count fell to 0.529 on full data.
        Top 5 authors hold 54.6% of positives. Summary: ml/data/rerun_after_backport_fix.txt
[ ] B4 (old text). Step 11 gate — FINAL RE-RUN NEEDED after final labels. A background
        job runs ml.audit automatically once label_summary.txt is written and
        saves ml/data/audit_summary.txt. Partial result (900 PRs, 3 repos):
        PASS, 8 features separate the classes. Re-check the per-repo and
        per-author sections on all 16 repos.

[ ] D0v. !! LABEL BUG FOUND AND FIXED 2026-09-14 (full-data B3 inspection):
        BACKPORTS AND CHERRY-PICKS OF A PR COUNTED AS HOTFIXES FOR THAT SAME PR.
        Example: aiohttp#11290 was labelled risky only because of
        "[PR #11290/16703bb9 backport][3.13] Fix file uploads ..." — a copy of
        #11290 onto a release branch, which "cites" #11290. Same for pylint's
        "[Backport maintenance/4.0.x] ..." commits.
        Fix in ml/label.py: ignore hotfix evidence that is a backport
        ("backport" in the subject), a cherry-pick ("cherry picked from
        commit" in the body), or whose subject repeats the PR's own title.
        3 new tests. 355 copy commits now ignored.
        Impact on the full 16-repo labels:
          risky PRs 211 (4.6%) -> 163 (3.5%)
          aio-libs/aiohttp 41 -> 15, pylint-dev/pylint 24 -> 2, others unchanged
        tox-dev/tox stays at 40 (13.3%): that is NOT this bug. One maintainer
        (gaborbernat) wrote 174 of tox's 300 sampled PRs and 39 of its 40 risky
        ones, iterating fast on "fix(config)" changes. Same-author label bias
        (D0o), now the largest single source of positives.
        Everything the automatic chain produced at 11:58 (labeled_prs.jsonl,
        dataset.csv, audit_summary.txt, train_summary.txt, backend/ml/model.json,
        feature_order.json, metrics.json, experiments_summary.txt) used the
        BUGGY labels — superseded. A re-run with the fix was started; its log is
        ml/data/rerun_after_backport_fix.txt. DO NOT COMMIT the 11:58 artifacts.
        NOTE: 3.5% is now BELOW the Step 10 gate's 5% floor.

[~] B19. Step 25 deploy — REPO PREPARED AND VERIFIED LOCALLY (2026-09-14); the live
        deploy needs your accounts, dashboards and a push.
        1. Neon: DATABASE_URL is the POOLED host (-pooler), sslmode=require, reachable.
        2. Backend prep:
           - backend/Procfile added; backend/.python-version = 3.13 (the venv's version).
           - requirements.txt pinned from pip freeze (50 packages). Verified: installs
             into a FRESH venv, `pip check` clean, and the Procfile command boots it
             on $PORT with /health 200 and a Neon read 200. (macOS only — Docker was
             not running, so a Linux build is unverified. On Linux, xgboost also
             downloads nvidia-nccl-cu12, ~300 MB; `xgboost-cpu` avoids it if the
             image is too big.)
           - model.json, feature_order.json, metrics.json: committed, not ignored,
             unchanged since commit.
           - CORS: now from CORS_ORIGINS env (comma-separated, trailing "/" stripped),
             default http://localhost:5173. Verified live: the configured origin is
             allowed and any other is refused. Added to .env.example.
           - FIXED a deploy trap: startup seeded the default user only when the users
             table was EMPTY, so a DEFAULT_USER_EMAIL on Railway that differed from
             the one already in Neon would make every route 500. Now it seeds by email
             (database.seed_default_user).
        3. Frontend prep: frontend/vercel.json rewrite for deep links; api.js strips a
           trailing "/" from VITE_API_URL. Lint clean, build OK.
        Tests: 197 pass (+test_deploy.py).
        ENV VARS THE PLAYBOOK LIST MISSES (without these the deploy half-works):
           - OPENAI_BASE_URL=https://api.groq.com/openai/v1 — otherwise the Groq key is
             sent to OpenAI, fails, and EVERY explanation silently uses the fallback.
           - CORS_ORIGINS=<your Vercel URL> — otherwise the browser blocks every call.
           - DEFAULT_USER_EMAIL must equal backend/.env's value, or the live app starts
             with an empty repository list (same Neon database).
        STILL TO DO: commit + push, Railway service, Vercel project, set CORS_ORIGINS,
        then live /health and one full prediction.

[x] B18. Step 24 hardening DONE (2026-09-14). 192 backend tests pass (was 158;
        +test_hardening.py). Offline subset: 163 tests, 2.9 s, sockets blocked.
        1. 404s: unknown UUIDs already gave 404 on all 5 id routes. FIXED two gaps:
           - malformed path id (/pull-requests/abc) gave a raw 422 pydantic list;
             now 404 "Pull request not found"/"Repository not found". Body ids
             (POST /predictions {"pull_request_id":"abc"}) stay 422, correctly.
           - GET /repositories/{id}/pull-requests for a repo deleted on GitHub gave
             502; now 404 "Repository owner/name no longer exists on GitHub…".
        2. Every `except` checked. requests: all caught in github._request/_json.
           openai: explain.py catches everything and falls back. FIXED: github
           _error_message crashed (AttributeError) on a JSON error body that is not
           an object, e.g. from a proxy; now falls back to the HTTP reason.
           NOT CHANGED: DB errors outside the pipeline's save (e.g. Neon down) reach
           the generic 500 handler as "Internal server error" — acceptable.
        3. FIXED: logging was never configured, so every services.* INFO line
           (incl. the pipeline timings) was silently dropped under uvicorn. Now one
           basicConfig in main.py, INFO, "%(asctime)s %(levelname)s %(name)s: …";
           uvicorn's own lines go through it too. Verified on a live server.
        4. /docs: added HealthOut + ErrorOut; 404/502/500 documented per route;
           removed the 422 FastAPI listed on path-only routes (can't happen now).
           Tests pin every route's body model and error codes.
        5. OK, no change: .env ignored (root .gitignore), never in git history;
           .env.example has all 9 keys config.py reads; frontend .env.example too.
        6. OK, no change: no real token in repo files or history. Scanned ghp_,
           github_pat_, gho_, sk-, gsk_ (Groq), npg_ (Neon), postgres://user:pw@.
           "sk-" hits are "risk-"/"mask-"; the only URL hit is the placeholder in
           .env.example. REMINDER A4: real values still live in backend/.env.
        7. OK, no change: checked in Neon itself (pg_indexes), not just models.py —
           repositories.user_id, pull_requests.repository_id,
           predictions.pull_request_id, predictions.created_at all indexed.
        TIMING (real GitHub + Neon + Groq, flask#6133, rolled back, 5 warm runs each):
           WITH LLM    median 2436 ms (2230-2508)   cold first run 2913 ms
           WITHOUT LLM median 1843 ms (1802-2078)
           stages: GitHub 1520-1600 | LLM ~560 | save 178 | load PR 90 |
                   author history 45 | score 3 | features 0
        PRD 800 ms target: MISSED by ~2.3x without the LLM, ~3x with it. See D6.

[x] B17. Step 23 gate DONE (2026-09-14). 158 backend tests pass (was 151).
        - tests/test_api.py (7 tests, TestClient + unittest.mock.patch): health 200;
          POST /repositories creates one row; twice -> still one row, same id;
          GitHub 404 -> 404 {"detail": "Repository not found or not public"} and no
          row; POST /predictions with services.github.get_pr/get_pr_files/
          get_pr_commits and services.explain.generate_explanation mocked -> 200,
          exactly one new prediction (author_pr_count 2.0, mocks called with
          acme/widgets/42); both history endpoints return that prediction; a test
          proving the suite can't reach api.github.com.
        - conftest.py now holds db (rolled-back Neon transaction) AND client
          (moved out of test_prediction_routes.py, no duplicate).
        - NO REAL NETWORK, ENFORCED: a session-wide autouse fixture refuses every
          Python-level hostname lookup except localhost/testserver. A test that
          forgets a mock fails with "network access is blocked in tests" instead
          of calling GitHub/Groq. Neon still works (libpq resolves in C).
        - Tests using `db` are auto-marked `db` (registered in pytest.ini).
        GATE, HONESTLY: "whole suite under 10 s with wifi off" is NOT met as
        written. Full suite = 16.6 s and its 22 db tests need Neon (a hosted DB),
        so they can't run offline. What IS proven: `pytest -m "not db"` = 136
        tests in 3.2 s with every Python socket blocked AND DATABASE_URL pointed
        at an unroutable host. 0 test rows (acme/*) left in Neon afterwards.
        TO GET THE FULL GATE: run a local Postgres for tests (e.g. TEST_DATABASE_URL
        -> localhost) — needs Postgres installed locally; not done, your call.

[x] B16. Step 22 gate DONE (2026-09-14). Lint clean, build OK. All six items
        verified in the browser against the real backend (8001) and frontend
        (5173), using real clicks and in-page stubs for empty/failing responses.
        1. EMPTY STATES now say what to do next:
           - PR list: "No pull requests found in this repository. Open one on
             GitHub, then check again." + a "Check again" button (verified: it
             shows the skeleton, then the real 30 PRs). "0 shown" removed (B14).
           - PR panel when NO repositories exist: "Connect a repository above to
             see its pull requests." (was an impossible "Select a repository").
           - History: "No analyses yet. Click Analyze above to score this pull
             request." Risk panel: "Not analysed yet. Click Analyze to fetch…".
           - Analysis saved with no measurements: "No measurements were saved
             with this analysis. Click Analyze again to record them." (verified
             by exact sentence).
           - Repositories: "No repositories connected yet — add one above"
             (already said what to do).
        2. LOADING + DISABLED while in flight: connect input is now read-only
           (greyed) and the button shows "Connecting…" disabled — previously
           a success cleared anything typed during the request. Analyze already
           verified in Step 21; Check again and Retry swap to the skeleton.
        3. ERRORS never blank, never a stack:
           - A non-JSON error body now reads "The server returned an error
             (HTTP 502)." instead of "Request failed" (verified with an HTML 502).
           - A failed Analyze shows the error ABOVE the last successful result,
             which stays visible (B15).
           - NEW src/components/ErrorBoundary.jsx around the routes: a crash
             while rendering (verified by feeding a PR with no repository) shows
             "Something went wrong on this page." + Reload page + Back to the
             dashboard; no stack or TypeError on the page; keyed by path, so
             navigating away clears it (verified). React has no hook for this,
             so it is the project's only class component — an intentional
             exception to CLAUDE.md's "plain functions" style.
           - React itself logs a caught render error to the console in
             development ("The above error occurred in the <PullRequestHeader>
             component…"). That is React's dev logging, not app code, and it is
             not shown on the page.
        4. 375px: no sideways page scroll on the dashboard or PR page; nothing
           off-screen; a long error message with a GitHub URL wraps (5 lines,
           inside the screen). The history table no longer scrolls inside its
           card: Model column hidden below sm (B15), and the Label column has no
           right padding below sm — the measured overflow was exactly that 16px
           (table 315px in a 309px card). Desktop keeps both.
           Long titles, repo names and explanations now wrap (break-words).
        5. KEYBOARD FOCUS: static audit — all 14 buttons, links and inputs in
           src/**/*.jsx have a focus-visible style. In the browser, real Tab
           presses through both pages: every stop matched :focus-visible with a
           solid 2px outline. FIXED: the SELECTED repository button had a dark
           outline on its dark background (invisible); it is now white. The
           "Page not found" link had no focus style; added.
        6. console.log: none anywhere in src (checked before and after). NEW lint
           rule "no-console": "error" in .oxlintrc.json — verified it fails on a
           deliberate console.log.
        No real data changed during the checks (the only real POSTs re-connected
        the existing pallets/flask row).
        Both dev servers are still running (backend 8001, frontend 5173).
        NOT committed yet: frontend/ polish (plus Steps 16-21).

[x] B15. Step 21 gate DONE (2026-09-14). 151 backend tests pass; frontend lint
        clean, build OK.
        Verified in the browser against the real backend (8001) and frontend
        (5173), on pallets/flask#6133:
        [x] Direct load of /pr/<id> (a full page load, so the data comes from
            the API, not the dashboard link): repo, #6133, merged badge, title,
            author, opened/merged dates, "View on GitHub" (new tab, rel
            noopener noreferrer).
        [x] The most recent saved analysis shows on arrival: RiskBadge
            "Low risk 0.20", "Analysed <date> · model xgb-v1", the explanation,
            and all 16 features in a fixed order. History table: Date, Score,
            Label, Model.
        [x] Analyze, with the click handler called TWICE before React could
            re-render (harsher than a real double-click): exactly ONE POST
            /predictions, and the database gained exactly one row (flask#6133:
            1 -> 2 saved predictions).
            Staged text on the timer: 0.5 s "Fetching PR data…" (spinner,
            aria-live="polite", button "Analyzing…" disabled, aria-busy="true");
            1.8 s "Scoring…"; 2.8 s "Generating explanation…". On success the
            result updated, the button came back as "Analyze again", history
            grew 1 -> 2.
        [x] Analyze failing (POST /predictions forced to 502 in the page): the
            API's message inline with Retry, button re-enabled, no stage text,
            nothing saved.
        [x] Unknown PR id: one "Pull request not found" + Retry + Back.
            BUG FOUND AND FIXED during the check: the error first appeared
            TWICE (header and history both failed for the same reason). The
            page now shows only Back and the one error when the PR cannot load.
        [x] Phone width 375px: no sideways page scroll; the history table
            scrolls inside its own card; the feature table fits.
        What was built:
          - src/components/RiskBadge.jsx: green / amber / red by label, the
            label written out (colour is never the only signal), score to two
            decimals.
          - src/components/ExplanationPanel.jsx: paragraph + a table of the
            features. Rows follow a fixed order because the backend stores
            features in a Postgres JSONB column, which does not keep key order.
          - src/components/HistoryTable.jsx: date, score, label, model version.
          - src/components/StateBadge.jsx: moved out of Dashboard.jsx so both
            pages share it. src/format.js: shared date and score formatting.
          - src/pages/PRDetail.jsx replaces the Step 20 placeholder. The
            double-click guard is a ref (updates immediately), not only the
            disabled attribute (which waits for a re-render). Stage timings
            follow the pipeline's measured stages.
        DIFFERENCE FROM THE STEP 21 PROMPT — a backend route was added:
          GET /pull-requests/{pr_id} -> the PR with its repository nested
          (404 for unknown ids). There was no API for one PR's metadata, so the
          page could not show its header on a refresh or direct link. Service:
          repo_sync.get_pull_request (history.pull_request_predictions now
          reuses it). 2 new tests. src/api.js gained getPullRequest.
        [x] DONE in Step 22: a failed Analyze now shows the error above the last
            result; on phones the history table fits its card (Model column
            hidden, Label column padding removed).
        LESSON: searching for `fetch(` also matches `refetch(` — the Step 21
          first search reported a false hit in Dashboard.jsx. Use a word
          boundary: grep -E '(^|[^A-Za-z0-9_.])fetch\('.
        Real data added in Neon: a second prediction for flask#6133.
        NOT committed yet: frontend/ and the backend route (plus Steps 16-20).

[x] B14. Step 20 gate DONE (2026-09-14). Lint clean, build OK.
        Verified in the browser (backend on 8001, frontend on 5173):
        [x] Connect form, client-side validation: "flask" -> inline "Enter the
            repository as owner/name, for example pallets/flask." — the network
            log shows NO request was sent.
        [x] Repo that does not exist -> the API's "Repository not found or not
            public" shown inline; the typed text stays for correcting.
        [x] "https://github.com/pallets/flask" -> connects (upsert of the
            existing row, no new data), input clears, repo selected, URL gets
            ?repo=<id>, 30 PRs with number, title, author, state badge and a
            GitHub link (new tab, rel="noopener noreferrer").
        [x] Clicking a PR row -> /pr/<id> (client-side); Back -> same ?repo,
            flask still selected, PRs reloaded.
        [x] The three required states, for BOTH panels:
            repositories — ERROR against the real backend (stopped): "Could not
            reach the API at http://localhost:8001. Is the backend running?" +
            Retry; Retry while still down keeps the error (no crash); Retry
            after restart recovers. EMPTY: "No repositories connected yet — add
            one above". LOADING on a fresh load: 3 skeleton rows + screen-reader
            "Loading repositories…".
            pull requests — LOADING: 6 skeleton rows, no stale rows from before;
            ERROR: API message + Retry; EMPTY: "No pull requests found in this
            repository."
        [x] Phone width 375px: no horizontal scroll, nothing off-screen, form
            stacks vertically, long titles truncate, GitHub link stays visible.
        What was built:
          - src/pages/Dashboard.jsx: connect form, repository list, PR list.
            The selected repository is kept in the URL (?repo=<id>) so Back
            from a PR returns to it. The form also accepts a pasted GitHub URL;
            validation mirrors the backend's rules. The GitHub link sits beside
            each row, not inside the row link (nested links are invalid HTML).
          - src/components/ErrorMessage.jsx and SkeletonRows.jsx, shared so
            Step 21 can reuse them.
          - src/App.jsx: header + routes (/, /pr/:id, and a "Page not found"
            page). src/main.jsx: BrowserRouter.
          - src/pages/PRDetail.jsx: a PLACEHOLDER showing the PR id and a Back
            button — Step 21 replaces it.
        DEPENDENCY NOTES:
          - react-router installed as v8.3.1 (npm's current), not v7 as the
            playbook assumes. The declarative API used here (BrowserRouter,
            Routes, Link, useSearchParams, useParams, useNavigate) works.
          - react-router is outside CLAUDE.md's stack list; added because the
            Step 20 prompt required it. CLAUDE.md's "Frontend:" line should
            list it so future sessions do not flag it (not edited — your file).
        [x] DONE in Step 22: "0 shown" no longer appears next to the empty message.
        HOW THE EMPTY / LOADING / ERROR STATES WERE TESTED (reuse in Step 22):
          the browser tool's scripts run in the page's own JavaScript world
          (they can see React's __reactFiber$ keys), so replacing window.fetch
          in the page reaches api.js. Trigger refetches through the app's own
          UI (connect, Retry, header link + repo click, PR row + Back) — a
          synthetic history.pushState + popstate pair does NOT remount the page.
          Browser tool batches are limited to 25 actions.
        Both dev servers are still running (backend 8001, frontend 5173).
        NOT committed yet: the whole frontend/ folder (plus Steps 16-18).

[x] B13. Step 19 gate DONE (2026-09-14).
        [x] The dev server serves a page that calls getRepos() through
            src/api.js and useApi and renders the raw JSON (pallets/flask).
            Backend log: GET /repositories 200 — the browser request from
            localhost:5173 to the API passed CORS.
        [x] With no backend running, the page showed "Could not reach the API
            at http://localhost:8001. Is the backend running?" plus Retry.
        What was built (frontend/):
          - Scaffolded with create-vite: React 19.2, Vite 8.3. Template demo
            page, App.css, sample images and public/icons.svg removed.
          - Tailwind 4.3 per the current Vite guide: the @tailwindcss/vite
            plugin in vite.config.js and @import "tailwindcss" in index.css.
            Tailwind v4 has NO tailwind.config.js — LLD v2 still lists one;
            update the LLD in Step 26.
          - src/api.js: the six functions. Also: a clear message when
            VITE_API_URL is unset or the backend is unreachable, and FastAPI's
            422 field-error lists turned into readable text instead of
            "[object Object]".
          - src/useApi.js: {data, loading, error, refetch}; only the latest call
            may update state, so a slow earlier response cannot overwrite a
            newer one.
          - frontend/.env (VITE_API_URL=http://localhost:8000) is gitignored by
            the root .gitignore; frontend/.env.example is committed instead.
          - src/index.css sets a light page background and dark text as the
            base style: in a dark-mode browser the first page had a dark
            background with nearly invisible dark text.
        "NO COMPONENT MAY CALL FETCH" IS ENFORCED BY LINT, not just a rule:
          .oxlintrc.json bans the fetch global everywhere except src/api.js
          (verified: a deliberate fetch in a component fails `npm run lint`).
          The template uses oxlint, not ESLint. GAP: `window.fetch(...)` is not
          caught — keep grepping for it.
        !! PORT 8000 IS TAKEN by another project: SpeechFlow AI Agent
          (~/Desktop/speechflow, uvicorn --reload, running since 2026-09-10).
          While it runs, a frontend using VITE_API_URL=http://localhost:8000
          talks to SPEECHFLOW, not this backend. Either stop SpeechFlow while
          working here, or run this backend on 8001 and set VITE_API_URL to
          match. Not touched.
        LOCAL DEV SERVERS: .claude/launch.json starts the backend on 8001 and
          the frontend on 5173 with VITE_API_URL overridden to 8001. It holds
          absolute paths for this Mac — do not commit it as is.
          The preview runner may not execute venv/bin/uvicorn from Desktop
          ("Operation not permitted", macOS provenance attribute); starting it
          as `./venv/bin/python -m uvicorn` works.
        Dev-only note: React StrictMode runs effects twice in development, so
          GET /repositories appears twice in the backend log. Production builds
          call it once.
        NOT committed yet: the whole frontend/ folder (plus Steps 16-18).

[x] B12. Step 18 gate DONE (2026-09-14). 149 tests pass (1 warning, from
        Starlette's own test client — a deprecated anyio alias, not our code).
        [x] Full flow on real GitHub, Groq and Neon through the API:
            GET /repositories -> GET /repositories/{id}/pull-requests ->
            POST /predictions (flask#6162, closed, Low 0.238) HTTP 200 in
            3,300 ms -> PR history 0 -> 1 and repository history 1 -> 2, the new
            prediction first in both.
        [x] /docs lists exactly: /health, /predictions,
            /predictions/pull-request/{pr_id}, /repositories,
            /repositories/{repo_id}/predictions, /repositories/{repo_id}/pull-requests
        [x] GET /debug/features/... -> 404.
        What changed:
          - main.py: POST /predictions, GET /predictions/pull-request/{pr_id},
            GET /repositories/{repo_id}/predictions — each only loads and
            delegates.
          - services/history.py (new): the two history queries, newest first,
            repository history limited to 50. Kept out of main.py per CLAUDE.md
            rule 4. Unknown PR or repository id -> 404, not an empty list (D0f).
          - tests/conftest.py (new): the rolled-back Neon session fixture plus
            repository and pull_request fixtures, now shared by test_pipeline.py
            and the new tests/test_prediction_routes.py (11 tests).
        Real data now in Neon, kept on purpose: predictions for flask#6133 and
        flask#6162.
        FOR STEP 24 / THE PRODUCT:
          - POST /predictions took 3,300 ms end to end (GitHub + Groq + Neon).
          - flask#6162 was CLOSED WITHOUT MERGING, and the API still scored it.
            Decide whether the dashboard should score closed-unmerged PRs at all
            (they will never be deployed), or show them as "not deployable".
          - Its explanation says the author "has no prior merged work in this
            repo" — that is the D0j artefact (only 30 recent PRs synced), not a
            fact about the author.
        NOT committed yet: main.py, services/history.py, removal of
        services/debug_features.py, tests/conftest.py, tests/test_pipeline.py,
        tests/test_prediction_routes.py (plus the Step 16-17 files).

[x] B11. Step 17 gate DONE (2026-09-14). 138 tests pass.
        [x] Real run on pallets/flask#6133 (real GitHub, Groq, Neon): exactly one
            predictions row inserted (0 -> 1); 7 stage timing lines + 1 total
            line. Low 0.195; the saved explanation is faithful. That row is kept
            in Neon on purpose as real prediction history.
        [x] Tests run against Neon inside a rolled-back transaction: verified
            no test rows (acme/widgets) were left behind.
        Differences from the Step 17 prompt, and why:
          - Unknown pull_request_id raises NotFoundError (404), not
            PredictionError (500): a bad id is the caller's mistake, and Step 06
            routes already return 404 for unknown ids (D0f). A PR deleted on
            GitHub is also 404. PredictionError is kept for a failed save.
          - 7 stage lines (load, fetch from GitHub, count author history, build
            features, score, explain, save) plus one total line giving the time
            with and without the explanation, for the Step 24 timing check.
          - The prediction row is added only after every stage succeeds; a
            failed commit is rolled back.
        STAGE TIMINGS on the real run (2,858 ms total):
          load 117 | GitHub 1,640 | author history 52 | features 0 | score 16 |
          explain 717 | save 314 ms -> 2,141 ms WITHOUT the explanation.
        !! Over the PRD's < 800 ms target (excluding the LLM). GitHub is 77% of
        it: get_pr, get_pr_files and get_pr_commits run one after another.
        Options for Step 24: fetch the three concurrently (thread pool; should
        roughly halve it), and state the budget as "excluding external APIs"
        in the report — the model itself takes 16 ms.
        Still open from this step:
          - B8 and D1 closed in Step 18 (see B12).
        NOT committed yet: services/pipeline.py, tests/test_pipeline.py (plus the
        Step 16 files listed in B10).

[x] B10. Step 16 gate DONE (2026-09-14) — ml/check_explanations.py, 3 rounds.
        10 cases: 7 real PRs spread across the score range (Low, Medium, High)
        plus 3 edge cases (empty PR, one-line README fix, huge weekend CI and
        config change). Automatic checks per explanation: numbers not in the
        prompt, alarming wording for the label, sentence count, markdown.
        ROUND 1 (Step 15 design: 5 features furthest from the mean):
          automatic checks 0/10 flagged, but READING found invented reasoning:
          - pylint#10275: prompt "changes configuration files: no (true for 18%
            of pull requests)" became "does not modify configuration files, a
            scenario that occurs in only 18%" (backwards) + "higher likelihood
            of unforeseen interactions" (invented).
          - sentry#4396: "well-reviewed ... vetted thoroughly" from 0 review
            comments. sphinx/streamlink: "well covered by tests", "ranges the
            model has seen". pylint#11046: included tests named a driver of
            the elevated score.
          Root cause: the prompt listed features that are UNUSUAL and asked the
          LLM to explain the SCORE, so unusual was read as causal. Also the
          relative-deviation measure capped "smaller than average" at 1.0 while
          any "no" on a yes/no feature always scored 1.0, so boring "no"s filled
          Low explanations and "1 file, 2 lines" never appeared.
        FIX (changes Step 15's design — same as Step 27's planned SHAP idea):
          - services/model.py contributions(): XGBoost's built-in tree SHAP
            values (pred_contribs), no new dependency. Test proves they add up
            exactly to the score.
          - explain.top_drivers(): the 5 features that moved THIS score most,
            each marked "raised the score" / "lowered the score".
          - Yes/no features phrased as the PR's actual state with the matching
            share: "does not change configuration files, like 82% of PRs".
          - Prompt forbids guessing about review quality, testing, the code,
            the author or the model's history.
        ROUND 2: no invented reasoning, all numbers and directions right, but
          ranking claims wrong in ~6/10 ("largest influence came from X" when X
          was not measurement 1); some listed all 5; some copied lines verbatim.
          Fix: numbered list, "measurement 1 had the largest effect", "discuss
          only the first 2 or 3 in that order", "do not mention unlisted
          measurements".
        ROUND 3 (final): order correct 10/10, directions correct 10/10, numbers
          correct 10/10, no alarming language for Low. The 2 automatic flags were
          false positives ("less risky") — checker fixed, test added.
        RESIDUAL ISSUES, accepted, worth mentioning in the report:
          - tox#4051 leaked instruction text: "...but we are only discussing the
            first two."
          - Mild paraphrase beyond the data: pylint#11046 called 46 prior PRs
            "the author's recent activity"; streamlink called a short commit
            message "a positive sign".
          - Prose is robotic ("The first measurement, ... lowered the score").
          - Explanations faithfully expose MODEL quirks that read oddly to a
            user: "0 hours between opening and merging raised the score" (empty
            PR), "13 lines removed (average 157) raised the score", and
            author_pr_count is a top-3 driver in 6/10 — the label-biased feature
            (D0o). Another reason to drop author_pr_count.
        SUGGESTED FOLLOW-UPS (not done):
          a) Production guardrail: in generate_explanation, reject any LLM reply
             containing a number that is not in its prompt and use the
             fallback. ml/check_explanations.numbers_not_in already does the
             check.
          b) Drop author_pr_count and retrain (D0j, D0o, and the explanations).
        NOT committed: services/model.py (contributions), services/explain.py,
        tests/test_model.py, tests/test_explain.py, ml/check_explanations.py,
        tests/test_check_explanations.py.

[x] B9. Step 15 gate DONE (2026-09-14). 126 tests pass.
        NOTE: Step 16 replaced the driver selection below (deviation from the
        mean) with the model's own contributions — see B10.
        [x] Live Groq call on the obviously-risky PR (score 0.366, Medium):
            "...changes 50 files and adds 1,980 lines, both well above the
            averages of 5.7 files and 347 lines added... moderately elevated".
            Every number quoted matches the prompt; ~0.9 s.
        [x] Deliberately invalid API key -> deterministic fallback in 233 ms, no
            exception; the warning logs Groq's 401 message, never the key (unit
            test checks the configured key is absent from logs).
        Differences from the Step 15 prompt, and why:
          - Training means are READ from metrics.json at import, not pasted
            into explain.py. A pasted copy would silently go stale after the
            next retrain. Import fails loudly if the means are missing.
          - "Largest deviation" = |value - mean| / mean, so large counts like
            lines_added do not always outrank yes/no features. metrics.json
            has no standard deviations, so a z-score was not possible.
          - New setting LLM_REASONING_EFFORT (default "low", in config.py and
            .env.example). Required for gpt-oss (A3: without it the reply is
            empty); leave empty for models that reject the parameter.
          - Explicit 20-second timeout and 1 retry. The openai client default
            is a 600-second timeout, which would hang a prediction.
          - Empty or truncated replies (finish_reason not "stop") also use the
            fallback, not only API errors.
          - The OpenAI client is created on first use inside the try, so a bad
            configuration becomes a fallback rather than an import error.
        FOR STEP 16 (faithfulness check):
          - The safe-PR reply named 4 measurements; the prompt asks for 2-3.
          - author_pr_count can appear as an explanation driver although it is
            the label-biased feature (D0o). If it is kept in FEATURE_ORDER,
            consider leaving it out of explanations.
        Committed 2026-09-14 together with Step 14 (see C4).

[ ] B8. Step 14 gate — HALF DONE.
        [x] Fake feature in FEATURE_ORDER -> services.model import fails
            loudly (verified: RuntimeError naming 'fake_feature').
        [x] predict() latency 0.47 ms per call (target < 100 ms). Import takes
            ~0.9 s once, at startup.
        [x] tests/test_model.py::test_trained_model_scores_obviously_risky_above_obviously_safe
            PASSES on the fixed-label full-data model (110 tests pass).
            is SKIPPED until backend/ml/model.json exists. After the final
            ml.train run:  cd backend && ./venv/bin/pytest tests/test_model.py -rs
            WARNING: on the Step 13 scratch model (9 trees, 6 repos) this test
            would FAIL — risky 0.442 vs safe 0.453. It DID FAIL on the 11:58
            full-data model (buggy labels, D0v): risky 0.184 vs safe 0.301.
            Re-run after the fixed-label retrain. The data does not support
            parts of the "obvious" intuition: in the Step 11 audit has_tests was
            slightly HIGHER for risky PRs, touches_config slightly LOWER, and
            is_weekend showed no signal. If it still fails on the final model,
            that is a finding to report, not a reason to hand-tune the model.
        [x] App fails loudly at startup — VERIFIED in Step 18: main.py imports
            services.pipeline, which loads the model; importing main with a fake
            feature in FEATURE_ORDER raised the feature-order RuntimeError.

[x] B7. Step 13b gate DONE via option 2: no configuration clears 0.75 in
        leave-repos-out CV. Best pooled 0.663 (14-day window + repo percentiles,
        XGBoost), +0.023 over current rules. 7 -> 16 repos did not raise the
        ceiling (0.644 -> 0.640). Reverts only: 13 positives, ~random.
        docs/model-ceiling.md rewritten with final numbers (preliminary banner
        removed). Summary: ml/data/experiments_summary.txt
[ ] B7 (old text). Step 13b gate — FINAL NUMBERS NEEDED after the full run. A background
        job runs ml.experiments (with and without author_pr_count) after the
        final training and saves ml/data/experiments_summary.txt. Then update
        the tables in docs/model-ceiling.md from it and remove its
        "preliminary" banner. Gate option 2 (written ceiling paragraph) is
        drafted in that file.

[~] B6. Step 13 results (fixed labels, 13 training repos, 137 risky):
                     held-out   CV mean ± std
          baseline     0.734    0.667 ± 0.060
          XGBoost      0.795    0.650 ± 0.068
        Gate "XGBoost beats baseline": YES on the held-out split, NO in CV.
        Gate "ROC-AUC >= 0.75": only on the single held-out split (26 risky
        PRs); CV says ~0.65. Treated as NOT met -> Step 13b (B7).
        Early stopping chose 29 trees, but validation PR-AUC 0.028 ~ random
        0.026: tree count effectively arbitrary.
        Held-out variants: baseline + log1p counts 0.803; without
        author_pr_count baseline 0.679, XGBoost 0.764.
        backend/ml/model.json, feature_order.json, metrics.json are now the
        FIXED-label artifacts — commit them together with the label.py fix.
[ ] B6 (old text). Step 13 gate + COMMIT — the same background job (B5) now also trains
        XGBoost and writes the REAL artifacts to backend/ml/: model.json,
        feature_order.json, metrics.json (plus ml/data/train_summary.txt).
        Then:
          1. Read train_summary.txt. Gate: XGBoost must beat the baseline.
             If XGBoost ROC-AUC >= 0.75 go to Step 14, otherwise Step 13b.
             Judge on the cross-validation mean, not only the single
             3-repo hold-out (see D0s).
          2. Commit the three artifact files (user asked for this in
             Step 13). Git rule for this project's assistant: branch first
             when on main, unless told to commit to main.
          3. NOTE: committing only the 3 JSON files does not make the report
             reproducible — the code that produces them (Steps 02-13) is
             still uncommitted (see C1). Commit the code too.
        Smoke-test artifacts were written to a scratch folder only; the real
        backend/ml/*.json files do not exist yet. They are not gitignored
        (checked); ml/data/ is.

[x] B5. Step 12 DONE: baseline on held-out aiohttp/pytest/wagtail (900 PRs,
        26 risky): ROC-AUC 0.734, PR-AUC 0.066 (random 0.029). CV 0.667 ± 0.060.
[ ] B5 (old text). Step 12 gate — REAL RUN NEEDED after final audit. A background job
        runs ml.train automatically once audit_summary.txt is complete and
        saves ml/data/train_summary.txt, using the default held-out repos
        pytest-dev/pytest, wagtail/wagtail, aio-libs/aiohttp. Write the
        baseline ROC-AUC down — it is the floor Step 13 must beat.

================================================================
C. GIT — nothing committed or pushed yet
================================================================

[x] C1. DONE 2026-09-14 — Steps 02-13b committed in ONE commit on branch
        steps-02-13-backend-ml-pipeline (created from main). NOT pushed.
        Before committing: 91 tests passed, and all 27 files were scanned for
        credentials (the leaked Neon/GitHub values and any token-shaped
        strings) — none found. backend/.env, venv/, ml/data/ are gitignored.
        NOT in this commit: backend/ml/model.json, feature_order.json,
        metrics.json — they are written after collection finishes (B6) and
        need their own commit.
        UPDATE: Harsh merged it into main and pushed — origin/main is at the
        Steps 02-13b commit (fb4b6f9). The branch can be deleted:
          git branch -d steps-02-13-backend-ml-pipeline

[x] C3. DONE 2026-09-14 — second commit, on main (local only, NOT pushed):
        the fixed-label model artifacts (backend/ml/model.json,
        feature_order.json, metrics.json), the label.py backport fix and its
        tests, docs/model-ceiling.md with final numbers, and this file.
        Credential scan on those files was clean before committing.
        Step 14 was committed separately afterwards (C4).
        To publish:  git push

[x] C4. DONE 2026-09-14 — third commit, on main (local only, NOT pushed):
        Steps 14-15 — services/model.py, services/explain.py,
        tests/test_model.py, tests/test_explain.py, config.py
        (LLM_REASONING_EFFORT), .env.example, and this file. 126 tests passed and
        the credential scan was clean before committing (its one match was the
        user:password@host placeholder in .env.example).
        To publish all unpushed commits:  git push

[ ] C2. Decide commit cadence: one commit per playbook step is recommended
        so the history reads as the build log for the report.

================================================================
D. SCHEDULED FOR LATER STEPS — do not do these early
================================================================

[x] D0. DONE in Step 07. features.py takes commit_count from get_pr()["commits"],
        NOT len(get_pr_commits()). GitHub stops listing commits after 250
        per PR (and files after 3,000), so the list undercounts huge PRs.
        Noted in comments in services/github.py.

[x] D0b. DONE in Step 08 (2026-09-14). File pagination verified on a REAL
        417-file PR, hawkrives/gelite#66: 5 pages fetched, 417 files listed,
        matching github.com's "Files changed 417".

[x] D0c. DONE in Step 06. services/repo_sync.py ALREADY EXISTS (created in Step 05
        for connect_repository / list_repositories). Add the PR upsert
        function to that file — do not create a second one.

[ ] D0d. Neon DB now holds one real row: pallets/flask in repositories.
        Left on purpose — Step 06 needs a connected repo.

[ ] D0e. GET /repositories/{id}/pull-requests returns EVERY stored PR for the
        repo, not just the latest 30 it syncs. Rows accumulate across syncs
        (by design — old PRs keep their prediction history). If the list
        gets long, add a limit or pagination during the Step 22 polish pass.

[ ] D0f. Step 06 added NotFoundError (exceptions.py) with a 404 handler, and
        moved Step 05's GitHub-404 mapping out of the route into
        repo_sync.connect_repository. Step 18 routes and the Step 24
        hardening pass should raise NotFoundError for unknown ids.

[x] D0g. DONE in Step 09 — repos vetted, flask and requests excluded. Step 09 repo choice: in pallets/flask's latest 30 PRs, only 1 was
        merged and 29 were closed without merging (seen live 2026-09-14).
        The playbook lists flask as an example training repo, but Step 09
        collects MERGED PRs only, so flask would yield very little data.
        Before collecting, check each candidate repo's merged-PR ratio and
        prefer repos that merge most PRs through GitHub.

[x] D0h. DONE — collect.py (Step 09) and services/pipeline.py (Step 17) both use get_pr(). !! CRITICAL FOR STEP 09 (collect.py) AND STEP 17 (pipeline.py):
        pass the FULL PR object from github.get_pr() into
        build_feature_vector — NEVER the item from list_pull_requests().
        The list endpoint omits changed_files, additions, deletions, commits
        and review_comments. features.py falls back to counting the file and
        commit lists for most of those, but review_comment_count silently
        becomes 0.0 for every PR. If training uses list items and serving
        uses get_pr(), the model learns review_comment_count is always 0 and
        then sees real values live: training-serving skew.
        collect.py must call get_pr() for every PR it saves (one extra API
        call per PR — budget for it in the rate-limit estimate).

[ ] D0i. Step 07 feature decisions to mention in the report / viva:
        - Test and config detection matches whole file and folder names, not
          substrings, so latest_version.py is not a "test" and
          environment.py is not "config".
        - test_file_ratio divides by the number of LISTED files.
        - pr_age_hours for a PR closed without merging stops at closed_at
          (the table only covered merged and open PRs).
        - Root-level files count as one directory "." in
          unique_dirs_touched.

[ ] D0k. EVIDENCE FOR D0h, found in Step 08. On hawkrives/gelite#66 GitHub's
        files API reported 0 added / 0 removed for 75 of 417 files (every one
        missing its "patch" — the diff was too big for the API). Summing the
        file list gave +29,515 -25,225; the true totals are +40,478 -34,756,
        about 27% short. features.py already prefers the PR totals, so the
        live features were correct. But if collect.py ever saved list items
        instead of get_pr() objects, lines_added / lines_removed /
        total_churn would silently undercount exactly the large, risky PRs
        the model most needs to learn. Filenames are still listed for those
        75 files, so has_tests / touches_config / unique_dirs are unaffected.
        The debug route's "all_three_match" is legitimately False for such
        PRs — compare feature_value against github_pr_total.

[ ] D0l. STEP 09 DATA DECISIONS — record these in the model card (Step 26):
        - 16 repos, chosen by a vetting pass over 36 candidates on
          2026-09-14 (stars, merge-through-GitHub ratio, human share,
          2-year merged volume). The list and reasons are in ml/collect.py.
          netbox (21.5k), mypy (20.6k) and wagtail (20.5k) are slightly above
          the playbook's 20k-star guideline; kept for data volume.
        - At most 300 PRs per repo, picked EVENLY across the 2-year window
          rather than the newest 300, so busy repos are not all recent PRs
          and no single repo dominates the dataset.
        - Only PRs merged into the default branch. Bot PRs (dependabot,
          renovate, pre-commit-ci...) excluded.
        - PRs merged in the last 14 days excluded: they have not had time to
          be reverted or hotfixed, so they would all be false "safe" labels.
        - Default-branch commits cover the WHOLE window, not "the last 500"
          as the playbook said. 500 commits reach back only weeks in busy
          repos, so every older PR could never be labelled risky.
        - Commit file lists are fetched only for fix/revert-like commits
          landing within 14 days after a collected PR's merge (saves several
          thousand API calls). Others have files = null.
        - Diff text ("patch") is dropped from stored files; features never
          read it.
        The collection window is pinned in ml/data/cache/window.json.

[x] D0m. DONE in Step 10 — Signal C dropped, hotfix window 7 days, merge-commit file lookup implemented. STEP 10 CONSTRAINTS created by Step 09:
        - Signal C (post-merge bug issues) was NOT collected — no issue data
          exists. Drop Signal C in label.py, or add an issues-collection step
          first. (Step 10's own advice already allows dropping it.)
        - The hotfix window in label.py must be <= 14 days. Commit file lists
          were only fetched up to 14 days after each merge; widening beyond
          that (e.g. Step 13b idea 1) needs a re-run with a larger
          LABEL_MATURITY_DAYS.
        - For a hotfix commit with files = null that is a merge commit of a
          collected PR, look its files up via that PR's merge_commit_sha.

[ ] D0n. STEP 10 LABEL METHODOLOGY — put this in the report / model card.
        Gate history on the same 900 PRs:
          21.4%  spec rules as written            -> hand-check: ~1-2 of 10 real
           9.1%  + source files only, no sweeps,
                   no hub files, no older fix PRs  -> ~2-3 of 10 real
           5.4%  + fix must be by the PR's author,
                   cite the PR (#N), or say
                   "regression"                    -> ~3-4 of 10 real (final)
        Hand-checked 8 same-author vs 8 other-author hotfix positives before
        adding the last rule: same-author ~6/8 real, other-author ~2/8 real.
        Deviations from the Step 10 prompt, each justified by inspection:
          - Signal C dropped: no issue data collected (D0m).
          - Hotfix subject word "patch" removed: meant mock/monkey-patching.
          - Only the commit SUBJECT line is checked for fix words — squash
            bodies are full of "* fix lint" lines.
          - Shared files must be SOURCE files: not tests, docs, changelog,
            lock, editor-config (.claude/.vscode...) or hub files touched by
            >=5% of the repo's PRs.
          - Fix commits touching >20 files (repo-wide sweeps) ignored.
          - Fix PRs numbered lower than the target were opened before it
            existed and cannot be its fix — ignored.
          - Fix must be by the PR's author, cite #PR, or say "regression".
        Signal A bug fixed during inspection: squash-merged revert subjects
        'Revert "X (#12)" (#15)' and GitHub-truncated '…' titles did not
        match; both now handled and tested.
        ESTIMATED LABEL PRECISION: roughly 50% (from ~28 hand-checked
        positives). Remaining false positives are mostly one author's
        planned series of fixes landing minutes apart in the same
        integration. No clean rule separates those from genuine quick
        "oops" hotfixes, so tightening further was stopped.
        CONSEQUENCE: noisy labels cap achievable ROC-AUC. The PRD target of
        >=0.75 may not be reachable. Tell the mentor in Week 5 (playbook
        Step 13b), not at the demo. Report PR-AUC too.

[ ] D0o. STEP 11 AUDIT MUST CHECK AUTHOR BIAS created by Step 10.
        The same-author rule means prolific authors who ship follow-up fixes
        get more risky labels. If author_pr_count (or any author-correlated
        feature) separates the classes strongly in Step 11, suspect the label
        rule rather than real risk. Print positive rate per author for the top
        authors in audit.py.

[ ] D0p. STEP 11 FINDINGS (partial data: 900 PRs from mypy, sentry-python,
        netbox). Decide the open questions BEFORE Step 12/13.
        - Saved ml/data/dataset.csv, not dataset.parquet: pandas needs pyarrow
          for parquet and pyarrow is not in the CLAUDE.md stack. CSV is fine at
          this size. If you want parquet, approve adding pyarrow — it is a
          one-line change in audit.py.
        - Single-feature ROC-AUC (0.5 = no signal): review_comment_count 0.644,
          author_pr_count 0.618, lines_added 0.612, commit_count 0.611,
          total_churn 0.607, files_changed 0.600, lines_removed 0.567,
          unique_dirs_touched 0.551. Timing and config features (is_weekend,
          is_off_hours, touches_ci, touches_config) show no signal (~0.50).
          No constant features.
        - Means mislead for size features: lines_added mean is LOWER for risky
          PRs (266 vs 410) because a few huge safe PRs inflate it, while its
          median and ROC-AUC are higher for risky. audit.py now prints medians.
          Explain this with medians in the report, not means.
        - sentry-python contributes 53% of positives — only 3 repos so far,
          re-check on all 16.
        - AUTHOR BIAS CONFIRMED (evidence for D0o): the 5 authors with most
          risky PRs hold 73.5% of all positives; some prolific authors sit at
          10-15% risky while others with 27-47 PRs have 0. author_pr_count's
          signal is probably largely an artefact of Step 10's same-author
          hotfix rule.
          RECOMMENDATION: drop author_pr_count from FEATURE_ORDER before
          Step 13. That also resolves D0j (training-serving skew) in one move.
          Alternative: keep it, but split train/test by author as well.
        - [x] LEAKAGE CHECKED, NOT A PROBLEM — keep review_comment_count.
          Probe 2026-09-14 on 40 PRs (20 risky, 20 safe): the feature equals
          GitHub's listed review comments 40/40. Only 3 of 150 comments were
          posted after merge (2%), and risky PRs had FEWER post-merge comments
          (1 of 98) than safe PRs (2 of 52). ROC-AUC on the sample was 0.679
          using all comments and 0.679 using pre-merge comments only. The
          signal comes from review discussion before merge, not from people
          reporting problems afterwards. Mention this check in the report.
        - SERVING-TIME QUESTION: does the dashboard score OPEN PRs or merged
          PRs awaiting deployment? For open PRs, pr_age_hours / is_weekend /
          is_off_hours use "now" instead of the merge time, and commit and
          review counts are still growing: a distribution shift from training
          on merged PRs. Decide and document.
        - Example worth discussing: netbox "Release v4.4.0" is labelled risky
          (hotfix followed). Release merges bundle many changes and are
          arguably the most deployment-like PRs — keep, but look at them in
          Step 13 error analysis.

[ ] D0q. STEP 12 SMOKE TEST (2026-09-14, 1,500 PRs from 5 repos) — the
        pipeline works; the NUMBERS ARE NOT MEANINGFUL YET.
        - Trained on 2 repos (600 PRs, 37 risky), held out moto, sqlfluff and
          netbox (900 PRs, only 18 risky): ROC-AUC 0.464, PR-AUC 0.018
          (random = 0.020). Leave-repos-out CV: 0.557 ± 0.080 over the 4
          folds with >= 5 risky PRs. Near random.
        - moto's CV fold had 1 risky PR and scored 0.973 — pure noise.
          train.py now prints risky counts per fold and excludes folds with
          fewer than 5 risky PRs from the mean.
        - Dropping author_pr_count LOWERED held-out ROC-AUC (0.464 -> 0.305),
          but with 18 test positives that is not evidence either way. Decide
          D0j/D0o on the full dataset, not on this run.
        - Collinear pair: test_file_ratio (-0.96) and has_tests (+0.80) have
          opposite coefficients. Expected with few positives; do not
          interpret coefficient signs in the report until the full run.

[x] D0u. RESOLVED 2026-09-14 — keep the fixed thresholds (< 0.34 Low,
        < 0.67 Medium, else High). The "94% Medium, 0% High" problem was
        specific to the 9-tree Step 13 scratch model. On the final model:
          all PRs:  Low 47.8% (0.1% actually risky) | Medium 48.5% (4.7%) |
                    High 3.7% (32.7%)
          held-out: Low 49.9% (0.4%) | Medium 49.2% (5.0%) | High 0.9% (25.0%,
                    only 2 of 8 PRs)
        The percentile alternative (Low = bottom 50%, High = top 10% of
        training scores) did WORSE on held-out repos: High 7.1% of PRs but only
        7.8% actually risky, barely above Medium's 5.2%.
        Caveat for the report: on repositories the model never saw, "High" is
        rare (8 of 900 PRs), and the "all PRs" figures include training repos,
        so they are optimistic. Still describe the score as a relative risk
        ranking, not a probability of failure.
[ ] D0u (old text, superseded). !! RISK LABEL THRESHOLDS DO NOT FIT THE MODEL'S SCORES (Step 14).
        Fixed thresholds < 0.34 Low, < 0.67 Medium, else High assume calibrated
        probabilities. The model's scores are not: XGBoost's scale_pos_weight
        (~18) pulls every score towards the middle. On the Step 13 scratch model,
        over 1,800 labelled PRs:
          scores 0.31 to 0.64 (median 0.43)
          Low     99 PRs ( 5.5%)  actually risky 0.0%
          Medium 1701 PRs (94.5%) actually risky 5.6%
          High     0 PRs ( 0.0%)
        The dashboard would call almost every PR "Medium" and never "High".
        Re-check on the final model; expect the same shape.
        Options (decide before Step 15/21, which show the label):
          a) RECOMMENDED: thresholds from the training-score distribution,
             e.g. Low = bottom 50% of scores, High = top 10%, saved in
             metrics.json by ml/train.py and read by services/model.py. Honest:
             "High" then means "riskier than 90% of PRs in the training data".
          b) Calibrate probabilities (Platt or isotonic) on the validation
             repos, keep fixed thresholds. Needs more positives than we have
             to calibrate reliably.
          c) Keep the fixed thresholds as the Step 14 prompt specified, and
             accept a nearly all-Medium dashboard.
        The 0-1 score itself should also be described as a relative risk
        score, not a probability of failure.

[ ] D0t. STEP 13b RESULT (2026-09-14, 2,100 PRs from 7 repos) — no fix clears
        0.75; gains are under +0.03. Write-up: docs/model-ceiling.md.
        Best pooled leave-repos-out ROC-AUC (baseline; XGBoost always lower):
          current 0.644 | 14-day window 0.652 | repo percentiles 0.635 |
          both 0.649 | reverts only: 5 risky PRs, unscorable.
          Without author_pr_count: 0.588 | 0.608 | 0.589 | 0.619.
        CEILING ARGUMENT (key viva point): with label precision p, even a
        perfect model scores at most ROC-AUC 0.5 + p/2. Hand-checked
        precision ~35-50% -> ceiling ~0.68-0.75, before missed positives.
        Observed 0.65 is just under it. Better labels, not a better model,
        are needed to go higher.
        DECISIONS FOR YOU AND THE MENTOR (tell the mentor NOW, Week 5):
          a) Accept ~0.65 and report it honestly with docs/model-ceiling.md.
          b) Ship the logistic-regression baseline instead of XGBoost? It
             scores higher everywhere. Step 14 (services/model.py) is built
             around XGBoost model.json — switching changes Step 14's design.
             The PRD says XGBoost; the report can show the comparison.
          c) Adopt the 14-day hotfix window? +0.008 to +0.020 and 27% more
             risky examples, but it changes the label definition. Cheap to
             adopt (label.py HOTFIX_WINDOW_DAYS = 14).
          d) Repo-relative size features: help XGBoost only. Shipping them
             needs a per-repo reference distribution at serving time
             (extra GitHub calls for any new repo). Not worth it for the
             baseline.
          e) Fix 5 (reverts only) cannot be tested without a separate
             collection targeted at reverted PRs. Not recommended in the
             remaining weeks.
          f) Fix 4 (more repos) is judged on the full 16-repo run; going to
             25 repos means several more hours of collection.

[ ] D0s. STEP 13 SMOKE TEST (2026-09-14, 1,800 PRs from 6 repos; trained on
        sentry-python, netbox, mypy; held out aiohttp, moto, sqlfluff).
        NOT FINAL — only 3 training repos and 49 training positives.
                         baseline   XGBoost
          accuracy         0.739     0.816   (all-safe would score 0.948)
          precision        0.127     0.106
          recall           0.681     0.340
          ROC-AUC          0.786     0.631
          PR-AUC           0.204     0.095   (random 0.052)
          leave-repos-out CV ROC-AUC: baseline 0.628 ± 0.070,
                                      XGBoost  0.602 ± 0.084
        - XGBoost did NOT beat the baseline here. With this few positives
          and noisy labels a linear model generalises better than trees.
          Re-judge on the full run before concluding.
        - The single hold-out ROC-AUC (0.786) is much higher than the CV mean
          (0.628): this particular hold-out set happens to be easier. Report
          the CV mean as the headline number, the hold-out as one sample.
        - Early stopping picked only 9 of 300 trees. The validation set was 1
          repo (mypy, 11 risky PRs) — too small to steer early stopping. On
          the full run validation becomes ~3 repos. If it still stops very
          early, choose the tree count by cross-validation across training
          repos instead of a single validation carve.
        - author_pr_count decision (D0j/D0o), better evidence now (47 test
          positives): baseline 0.786 -> 0.775 without it, XGBoost 0.631 ->
          0.586. Dropping it costs little for the baseline and removes the
          training-serving skew and label-bias problems. Still recommended.
        - Implementation choices in train.py: validation repos are carved from
          TRAINING repos only (never test); after early stopping the final
          model is refit on all training repos with exactly the chosen tree
          count, so model.json needs no best_iteration handling in Step 14.
          metrics.json also stores training-set feature_means for Step 15.
          train.py refuses to save artifacts when --drop is used, because the
          saved feature order would not match features.FEATURE_ORDER.
        - Reload check passed: model.json reloaded gives the same held-out
          ROC-AUC as metrics.json (0.631), 9 trees.

[ ] D0r. !! LABEL RATE DEPENDS ON REPO CULTURE, NOT ONLY RISK (Step 10 impact,
        found in Step 12). Per-repo risky rates: sentry-python 8.7%, netbox
        4.0%, mypy 3.7%, sqlfluff 1.7%, moto 0.3%. Overall rate fell to 3.7%
        with 5 repos — BELOW the Step 10 gate's 5% floor.
        Why moto is so low:
          - only 9% of its commit subjects contain a fix word
            ("EC2: fix bad test assertion" style is rarer than sentry's
            "fix(scope): ..."), versus 21-25% for sentry and mypy;
          - 97 distinct authors in its 300 PRs, 67 of them with a single PR,
            so the same-author hotfix rule can rarely fire.
        Consequence: the label means something different in each repo, so a
        model trained on some repos transfers poorly to others. This is a
        likely reason cross-repo ROC-AUC is near 0.5.
        Options to discuss with the mentor before Step 13:
          a) accept, and report per-repo results plus this limitation;
          b) also evaluate WITHIN repos (time-based split per repo) to show
             whether the features carry signal when the label definition is
             consistent — keep the cross-repo split as the headline;
          c) drop repos whose labels barely fire (e.g. <2% risky) and say so.
        Re-check all of this on the full 16-repo run (B3/B4/B5).

[ ] D0j. !! author_pr_count TRAINING-SERVING SKEW — NOW SEEN LIVE (Step 17).
        The real pipeline run on pallets/flask#6133 computed author_pr_count = 0
        because Neon holds only flask's 30 most recent PRs; the training mean
        is 33. So live, this feature pushes almost every score DOWN (the saved
        explanation even names it: "0 (average 33) ... lowered the score").
        Dropping the feature and retraining fixes this, D0o and the Step 16
        explanation problem at once.
        (original note follows)
        TRAINING-SERVING SKEW RISK (Step 09 vs Step 17).
        Serving (repo_sync.count_prior_merged_prs, per the Step 17 plan)
        counts only PRs stored locally — the latest 30 synced per repo — so
        live values will be small. Training (Step 09) would count from ~2
        years of collected PRs, so training values will be much larger.
        Same feature, different data source = the model is miscalibrated
        on this feature in production.
        Fix before Step 13 — pick ONE source for both:
          a) GitHub search API, 1 call per PR:
             q=repo:OWNER/NAME is:pr is:merged author:LOGIN merged:<CREATED_AT
             and use total_count — works identically in training and serving
          b) or drop author_pr_count from FEATURE_ORDER.

[x] D1. DONE in Step 18 — route, services/debug_features.py and its import removed;
        searched the backend, no references left. repo_sync.get_repository and
        count_prior_merged_prs kept (the pipeline and history service use them).
        Remove the Step 08 debug route. It lives in TWO places, both marked
        "# TODO: remove before deployment":
          - main.py: GET /debug/features/{repo_id}/{pr_number}
          - services/debug_features.py (delete the whole file)
        Also drop "debug_features" from the services import in main.py.
        Keep repo_sync.get_repository and repo_sync.count_prior_merged_prs —
        Step 17 reuses them.
        Scheduled for Step 18.

[ ] D2. Pin requirements.txt with `pip freeze`.
        Step 25, right before deploying. Unpinned is fine until then.

[ ] D3. Alembic migrations.
        Deferred to Week 8 by LLD v2 §2 on purpose — the schema churns hardest
        in Weeks 3-5 and there is no data to protect yet. Using
        create_all() until then.

[ ] D4. Authentication (JWT, /auth/register, /auth/login, hashed_password).
        Deferred entirely — not in the PRD Must-Have list. Single-user mode
        via DEFAULT_USER_EMAIL. Upgrade path is in LLD v2 §2 if time allows
        in Weeks 9-10.

[ ] D5. Update LLD.md to match whatever the code actually became.
        Step 26.

[ ] D6. PIPELINE IS ~2.4 s, PRD TARGET 800 ms (measured in Step 24, B18). YOUR CALL.
        Where the time goes: GitHub 1.5-1.6 s (get_pr, files, commits run one
        after another), LLM ~560 ms, Neon ~45 ms per round trip (save 178, load 90).
        Options, cheapest first:
        a) Fetch the 3 GitHub calls concurrently (thread pool): est. -1.0 s.
        b) Load the PR and the default user in one query: est. -45 ms.
        c) Return the score first and fill in the explanation afterwards: removes
           ~560 ms from the response, but needs an extra endpoint or polling.
        Even with a)+b), "without LLM" lands around 800-900 ms, so the honest
        report line is probably "~0.8-0.9 s without the explanation, ~1.4 s
        with it" rather than "under 800 ms".
        The timing script (real GitHub/Neon/Groq, rolled back) is in the session
        scratchpad, not the repo — ask if you want it kept as backend/ml/time_pipeline.py.

================================================================
E. PROJECT ADMIN — not code
================================================================

[ ] E1. PRD page 7 responsibilities table is blank. Nine work items have no
        names assigned, and signatures/date are unsigned. Needed for the
        mentor checkpoint.

[ ] E2. Decide with Shravani who owns which of the nine tasks.

================================================================
F. STRETCH GOALS — Weeks 9-10 only, if time allows
================================================================

[ ] F1. Step 27 — SHAP feature importance + ContributionBars component.
[ ] F2. Step 28 — Batch scan of all open PRs, ranked by risk.
[ ] F3. Model comparison view (LogReg vs XGBoost side by side).
[ ] F4. Risk trend charts per repository over time.
[ ] F5. Confidence calibration display.

================================================================
NOTES
================================================================
- Docker binary is installed on this machine but the daemon is not running.
- Python venv lives at backend/venv/ (gitignored). All 13 deps installed.
- backend/.env exists with PLACEHOLDER values so the app boots; it is
  gitignored and contains no real credentials.
